# Frontend

This directory contains the React frontend.

- `src/` - application source code
- `public/` - static public assets used by React
- `package.json` and `package-lock.json` - frontend dependencies and build scripts

The production build is generated into `frontend/build/` and served by `main.py` in Cloud Run.
